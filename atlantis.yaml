version: 3
automerge: true
parallel_plan: true
parallel_apply: true

projects:
  - name: apps-staging
    dir: .
    workspace: staging
    terraform_version: v1.6.6
    autoplan:
      enabled: true
      when_modified:
        - "application/**"
    workflow: matrix-tf

  - name: apps-production
    dir: .
    workspace: production
    terraform_version: v1.6.6
    autoplan:
      enabled: true
      when_modified:
        - "application/**"
    workflow: matrix-tf

workflows:
  matrix-tf:
    plan:
      steps:
        # Step 1: Detect changed subdirectories inside application/
        - run: |
            echo "🔍 Detecting changed application subdirectories..."
            CHANGED_DIRS=$(git diff --name-only $BASE_BRANCH...$HEAD | grep '^application/' | cut -d'/' -f1-2 | sort -u || true)

            if [ -z "$CHANGED_DIRS" ]; then
              echo "✅ No changes detected in application subdirectories."
              exit 0
            fi

            echo "🧩 Changed application directories:"
            echo "$CHANGED_DIRS"

            for dir in $CHANGED_DIRS; do
              echo "🚀 Running Terraform init for $dir"
              cd "$dir"
              terraform init -input=false -backend-config="./env/production/prod.conf"

              echo "📦 Running Terraform plan for $dir"
              terraform plan -input=false -no-color -var-file="./config/production.tfvars" -out=tfplan

              cd - > /dev/null
            done

    apply:
      steps:
        - run: |
            echo "🔍 Detecting changed application subdirectories..."
            CHANGED_DIRS=$(git diff --name-only $BASE_BRANCH...$HEAD | grep '^application/' | cut -d'/' -f1-2 | sort -u || true)

            if [ -z "$CHANGED_DIRS" ]; then
              echo "✅ No changes detected to apply."
              exit 0
            fi

            echo "🧩 Changed application directories:"
            echo "$CHANGED_DIRS"

            for dir in $CHANGED_DIRS; do
              echo "🚀 Applying Terraform for $dir"
              cd "$dir"
              terraform apply -input=false -auto-approve -var-file="./config/production.tfvars" tfplan || true
              cd - > /dev/null
            done
