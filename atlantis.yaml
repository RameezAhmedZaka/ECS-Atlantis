version: 3
automerge: false
parallel_plan: true
parallel_apply: true

# Pre-workflow hooks run for ALL projects
pre_workflow_hooks:
  - run: chmod +x ./scripts/process-application.sh
  - run: ./scripts/process-application.sh
    description: "Generating configuration files"



