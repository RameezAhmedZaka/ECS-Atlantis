version: 3
automerge: false 
parallel_plan: false  
parallel_apply: false  

projects:
  - name: apps-staging
    dir: .
    terraform_version: v1.6.6
    autoplan:
      enabled: true  
      when_modified:
        - "application/**"
    workflow: staging-workflow
    apply_requirements: [] 

  - name: apps-production
    dir: .
    terraform_version: v1.6.6
    autoplan:
      enabled: true  
      when_modified:
        - "application/**"
    workflow: production-workflow
    apply_requirements: [approved, mergeable] 

  - name: apps-helia
    dir: .
    terraform_version: v1.6.6
    autoplan:
      enabled: true 
      when_modified:
        - "application/**"
    workflow: helia-workflow
    apply_requirements: []     

workflows:
  staging-workflow:
    plan:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="staging"
            echo "DEBUG: Raw COMMENT_ARGS='$COMMENT_ARGS'"
            echo "DEBUG: Cleaned APP_FILTER='$APP_FILTER'"
            echo "Running Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/process-application.sh
            ./scripts/process-application.sh "$ENV" "$APP_FILTER"
        # Generate custom output that Atlantis will display in the main comment
        - run: |
            set -euo pipefail
            ENV="staging"
            PLANLIST="/tmp/atlantis_planfiles_${ENV}.lst"
            
            if [[ -f "$PLANLIST" ]]; then
              # Count apps and collect names
              APP_COUNT=0
              APP_NAMES=""
              
              while IFS='|' read -r d PLAN; do
                APP_NAME=$(basename "$d" | tr -d '[:space:]')
                if [[ -n "$APP_NAME" ]]; then
                  APP_COUNT=$((APP_COUNT + 1))
                  APP_NAMES="${APP_NAMES} $APP_NAME"
                fi
              done < "$PLANLIST"
              
              # Generate Atlantis custom output format
              cat << EOF

              **Changed Applications ($APP_COUNT):**
              $(for app in $APP_NAMES; do echo "- $app"; done)

              ---

              **Apply Commands:**

              EOF
              
              if [[ $APP_COUNT -eq 1 ]]; then
                APP_NAME=$(echo "$APP_NAMES" | tr -d '[:space:]')
                echo "▶️ To apply **$APP_NAME**, comment:"
                echo "   \`\`\`atlantis apply -p apps-staging -- $APP_NAME\`\`\`"
              else
                echo "▶️ To apply individual applications, comment:"
                for app in $APP_NAMES; do
                  echo "   - \`\`\`atlantis apply -p apps-staging -- $app\`\`\`"
                done
                echo ""
                echo "⏩ To apply **all $APP_COUNT applications**, comment:"
                echo "   \`\`\`atlantis apply -p apps-staging\`\`\`"
              fi
              
              cat << EOF

              🚮 To delete this plan and lock, click here
              🔁 To plan this project again, comment:
                 \`\`\`atlantis plan -p apps-staging\`\`\`
              EOF
            else
              # Fallback output
              cat << EOF

              ▶️ To apply this plan, comment:
                 \`\`\`atlantis apply -p apps-staging\`\`\`
              🚮 To delete this plan and lock, click here
              🔁 To plan this project again, comment:
                 \`\`\`atlantis plan -p apps-staging\`\`\`
              EOF
            fi

    apply:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="staging"
            echo "Applying Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/apply-plans.sh
            ./scripts/apply-plans.sh "$ENV" "$APP_FILTER"

  production-workflow:
    plan:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="production"
            echo "Running Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/process-application.sh
            ./scripts/process-application.sh "$ENV" "$APP_FILTER"
        # Generate custom output that Atlantis will display in the main comment
        - run: |
            set -euo pipefail
            ENV="production"
            PLANLIST="/tmp/atlantis_planfiles_${ENV}.lst"
            
            if [[ -f "$PLANLIST" ]]; then
              # Count apps and collect names
              APP_COUNT=0
              APP_NAMES=""
              
              while IFS='|' read -r d PLAN; do
                APP_NAME=$(basename "$d" | tr -d '[:space:]')
                if [[ -n "$APP_NAME" ]]; then
                  APP_COUNT=$((APP_COUNT + 1))
                  APP_NAMES="${APP_NAMES} $APP_NAME"
                fi
              done < "$PLANLIST"
              
              # Generate Atlantis custom output format
              cat << EOF

              **Changed Applications ($APP_COUNT):**
              $(for app in $APP_NAMES; do echo "- $app"; done)

              ---

              **Apply Commands:**

              EOF
              
              if [[ $APP_COUNT -eq 1 ]]; then
                APP_NAME=$(echo "$APP_NAMES" | tr -d '[:space:]')
                echo "▶️ To apply **$APP_NAME**, comment:"
                echo "   \`\`\`atlantis apply -p apps-production -- $APP_NAME\`\`\`"
              else
                echo "▶️ To apply individual applications, comment:"
                for app in $APP_NAMES; do
                  echo "   - \`\`\`atlantis apply -p apps-production -- $app\`\`\`"
                done
                echo ""
                echo "⏩ To apply **all $APP_COUNT applications**, comment:"
                echo "   \`\`\`atlantis apply -p apps-production\`\`\`"
              fi
              
              cat << EOF

              🚮 To delete this plan and lock, click here
              🔁 To plan this project again, comment:
                 \`\`\`atlantis plan -p apps-production\`\`\`
              EOF
            else
              # Fallback output
              cat << EOF

              ▶️ To apply this plan, comment:
                 \`\`\`atlantis apply -p apps-production\`\`\`
              🚮 To delete this plan and lock, click here
              🔁 To plan this project again, comment:
                 \`\`\`atlantis plan -p apps-production\`\`\`
              EOF
            fi

    apply:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="production"
            echo "Applying Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/apply-plans.sh
            ./scripts/apply-plans.sh "$ENV" "$APP_FILTER"

  helia-workflow:
    plan:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="helia"
            echo "Running Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/process-application.sh
            ./scripts/process-application.sh "$ENV" "$APP_FILTER"
        # Generate custom output that Atlantis will display in the main comment
        - run: |
            set -euo pipefail
            ENV="helia"
            PLANLIST="/tmp/atlantis_planfiles_${ENV}.lst"
            
            if [[ -f "$PLANLIST" ]]; then
              # Count apps and collect names
              APP_COUNT=0
              APP_NAMES=""
              
              while IFS='|' read -r d PLAN; do
                APP_NAME=$(basename "$d" | tr -d '[:space:]')
                if [[ -n "$APP_NAME" ]]; then
                  APP_COUNT=$((APP_COUNT + 1))
                  APP_NAMES="${APP_NAMES} $APP_NAME"
                fi
              done < "$PLANLIST"
              
              # Generate Atlantis custom output format
              cat << EOF

              **Changed Applications ($APP_COUNT):**
              $(for app in $APP_NAMES; do echo "- $app"; done)

              ---

              **Apply Commands:**

              EOF
              
              if [[ $APP_COUNT -eq 1 ]]; then
                APP_NAME=$(echo "$APP_NAMES" | tr -d '[:space:]')
                echo "▶️ To apply **$APP_NAME**, comment:"
                echo "   \`\`\`atlantis apply -p apps-helia -- $APP_NAME\`\`\`"
              else
                echo "▶️ To apply individual applications, comment:"
                for app in $APP_NAMES; do
                  echo "   - \`\`\`atlantis apply -p apps-helia -- $app\`\`\`"
                done
                echo ""
                echo "⏩ To apply **all $APP_COUNT applications**, comment:"
                echo "   \`\`\`atlantis apply -p apps-helia\`\`\`"
              fi
              
              cat << EOF

              🚮 To delete this plan and lock, click here
              🔁 To plan this project again, comment:
                 \`\`\`atlantis plan -p apps-helia\`\`\`
              EOF
            else
              # Fallback output
              cat << EOF

              ▶️ To apply this plan, comment:
                 \`\`\`atlantis apply -p apps-helia\`\`\`
              🚮 To delete this plan and lock, click here
              🔁 To plan this project again, comment:
                 \`\`\`atlantis plan -p apps-helia\`\`\`
              EOF
            fi

    apply:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="helia"
            echo "Applying Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/apply-plans.sh
            ./scripts/apply-plans.sh "$ENV" "$APP_FILTER"