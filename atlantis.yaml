version: 3
automerge: false 
parallel_plan: false  
parallel_apply: false  

projects:
  - name: apps-staging
    dir: .
    terraform_version: v1.6.6
    autoplan:
      enabled: true  
      when_modified:
        - "application/**"
    workflow: staging-workflow
    apply_requirements: [] 

  - name: apps-production
    dir: .
    terraform_version: v1.6.6
    autoplan:
      enabled: true  
      when_modified:
        - "application/**"
    workflow: production-workflow
    apply_requirements: [approved, mergeable] 

  - name: apps-helia
    dir: .
    terraform_version: v1.6.6
    autoplan:
      enabled: true 
      when_modified:
        - "application/**"
    workflow: helia-workflow
    apply_requirements: []     

workflows:
  staging-workflow:
    plan:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="staging"
            echo "DEBUG: Raw COMMENT_ARGS='$COMMENT_ARGS'"
            echo "DEBUG: Cleaned APP_FILTER='$APP_FILTER'"
            echo "Running Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/process-application.sh
            ./scripts/process-application.sh "$ENV" "$APP_FILTER"
        # Generate custom output file for Atlantis
        - run: |
            set -euo pipefail
            ENV="staging"
            PLANLIST="/tmp/atlantis_planfiles_${ENV}.lst"
            OUTPUT_FILE="/tmp/atlantis_custom_output_${ENV}.md"
            
            # Create custom output
            {
              echo "# Plan Summary - Staging"
              echo ""
              
              if [[ -f "$PLANLIST" ]]; then
                # Count apps and collect names
                APP_COUNT=0
                APP_NAMES=""
                
                while IFS='|' read -r d PLAN; do
                  APP_NAME=$(basename "$d" | tr -d '[:space:]')
                  if [[ -n "$APP_NAME" ]]; then
                    APP_COUNT=$((APP_COUNT + 1))
                    APP_NAMES="${APP_NAMES} $APP_NAME"
                  fi
                done < "$PLANLIST"
                
                echo "## Changed Applications ($APP_COUNT)"
                for app in $APP_NAMES; do
                  echo "- $app"
                done
                echo ""
                echo "---"
                echo ""
                echo "## Apply Commands"
                echo ""
                
                if [[ $APP_COUNT -eq 1 ]]; then
                  APP_NAME=$(echo "$APP_NAMES" | tr -d '[:space:]')
                  echo "▶️ To apply **$APP_NAME**, comment:"
                  echo ""
                  echo "\`\`\`"
                  echo "atlantis apply -p apps-staging -- $APP_NAME"
                  echo "\`\`\`"
                else
                  echo "▶️ To apply individual applications, comment:"
                  echo ""
                  for app in $APP_NAMES; do
                    echo "- \`atlantis apply -p apps-staging -- $app\`"
                  done
                  echo ""
                  echo "⏩ To apply **all $APP_COUNT applications**, comment:"
                  echo ""
                  echo "\`\`\`"
                  echo "atlantis apply -p apps-staging"
                  echo "\`\`\`"
                fi
              else
                echo "## Apply Commands"
                echo ""
                echo "▶️ To apply this plan, comment:"
                echo ""
                echo "\`\`\`"
                echo "atlantis apply -p apps-staging"
                echo "\`\`\`"
              fi
              
              echo ""
              echo "---"
              echo ""
              echo "🚮 To delete this plan and lock, click the 'Discard Plans' button"
              echo ""
              echo "🔁 To re-plan, comment:"
              echo ""
              echo "\`\`\`"
              echo "atlantis plan -p apps-staging"
              echo "\`\`\`"
              
            } > "$OUTPUT_FILE"
            
            echo "Custom output written to $OUTPUT_FILE"

    apply:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="staging"
            echo "Applying Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/apply-plans.sh
            ./scripts/apply-plans.sh "$ENV" "$APP_FILTER"

  production-workflow:
    plan:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="production"
            echo "Running Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/process-application.sh
            ./scripts/process-application.sh "$ENV" "$APP_FILTER"
        # Generate custom output file for Atlantis
        - run: |
            set -euo pipefail
            ENV="production"
            PLANLIST="/tmp/atlantis_planfiles_${ENV}.lst"
            OUTPUT_FILE="/tmp/atlantis_custom_output_${ENV}.md"
            
            # Create custom output
            {
              echo "# Plan Summary - Production"
              echo ""
              
              if [[ -f "$PLANLIST" ]]; then
                # Count apps and collect names
                APP_COUNT=0
                APP_NAMES=""
                
                while IFS='|' read -r d PLAN; do
                  APP_NAME=$(basename "$d" | tr -d '[:space:]')
                  if [[ -n "$APP_NAME" ]]; then
                    APP_COUNT=$((APP_COUNT + 1))
                    APP_NAMES="${APP_NAMES} $APP_NAME"
                  fi
                done < "$PLANLIST"
                
                echo "## Changed Applications ($APP_COUNT)"
                for app in $APP_NAMES; do
                  echo "- $app"
                done
                echo ""
                echo "---"
                echo ""
                echo "## Apply Commands"
                echo ""
                
                if [[ $APP_COUNT -eq 1 ]]; then
                  APP_NAME=$(echo "$APP_NAMES" | tr -d '[:space:]')
                  echo "▶️ To apply **$APP_NAME**, comment:"
                  echo ""
                  echo "\`\`\`"
                  echo "atlantis apply -p apps-production -- $APP_NAME"
                  echo "\`\`\`"
                else
                  echo "▶️ To apply individual applications, comment:"
                  echo ""
                  for app in $APP_NAMES; do
                    echo "- \`atlantis apply -p apps-production -- $app\`"
                  done
                  echo ""
                  echo "⏩ To apply **all $APP_COUNT applications**, comment:"
                  echo ""
                  echo "\`\`\`"
                  echo "atlantis apply -p apps-production"
                  echo "\`\`\`"
                fi
              else
                echo "## Apply Commands"
                echo ""
                echo "▶️ To apply this plan, comment:"
                echo ""
                echo "\`\`\`"
                echo "atlantis apply -p apps-production"
                echo "\`\`\`"
              fi
              
              echo ""
              echo "---"
              echo ""
              echo "🚮 To delete this plan and lock, click the 'Discard Plans' button"
              echo ""
              echo "🔁 To re-plan, comment:"
              echo ""
              echo "\`\`\`"
              echo "atlantis plan -p apps-production"
              echo "\`\`\`"
              
            } > "$OUTPUT_FILE"
            
            echo "Custom output written to $OUTPUT_FILE"

    apply:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="production"
            echo "Applying Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/apply-plans.sh
            ./scripts/apply-plans.sh "$ENV" "$APP_FILTER"

  helia-workflow:
    plan:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="helia"
            echo "Running Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/process-application.sh
            ./scripts/process-application.sh "$ENV" "$APP_FILTER"
        # Generate custom output file for Atlantis
        - run: |
            set -euo pipefail
            ENV="helia"
            PLANLIST="/tmp/atlantis_planfiles_${ENV}.lst"
            OUTPUT_FILE="/tmp/atlantis_custom_output_${ENV}.md"
            
            # Create custom output
            {
              echo "# Plan Summary - Helia"
              echo ""
              
              if [[ -f "$PLANLIST" ]]; then
                # Count apps and collect names
                APP_COUNT=0
                APP_NAMES=""
                
                while IFS='|' read -r d PLAN; do
                  APP_NAME=$(basename "$d" | tr -d '[:space:]')
                  if [[ -n "$APP_NAME" ]]; then
                    APP_COUNT=$((APP_COUNT + 1))
                    APP_NAMES="${APP_NAMES} $APP_NAME"
                  fi
                done < "$PLANLIST"
                
                echo "## Changed Applications ($APP_COUNT)"
                for app in $APP_NAMES; do
                  echo "- $app"
                done
                echo ""
                echo "---"
                echo ""
                echo "## Apply Commands"
                echo ""
                
                if [[ $APP_COUNT -eq 1 ]]; then
                  APP_NAME=$(echo "$APP_NAMES" | tr -d '[:space:]')
                  echo "▶️ To apply **$APP_NAME**, comment:"
                  echo ""
                  echo "\`\`\`"
                  echo "atlantis apply -p apps-helia -- $APP_NAME"
                  echo "\`\`\`"
                else
                  echo "▶️ To apply individual applications, comment:"
                  echo ""
                  for app in $APP_NAMES; do
                    echo "- \`atlantis apply -p apps-helia -- $app\`"
                  done
                  echo ""
                  echo "⏩ To apply **all $APP_COUNT applications**, comment:"
                  echo ""
                  echo "\`\`\`"
                  echo "atlantis apply -p apps-helia"
                  echo "\`\`\`"
                fi
              else
                echo "## Apply Commands"
                echo ""
                echo "▶️ To apply this plan, comment:"
                echo ""
                echo "\`\`\`"
                echo "atlantis apply -p apps-helia"
                echo "\`\`\`"
              fi
              
              echo ""
              echo "---"
              echo ""
              echo "🚮 To delete this plan and lock, click the 'Discard Plans' button"
              echo ""
              echo "🔁 To re-plan, comment:"
              echo ""
              echo "\`\`\`"
              echo "atlantis plan -p apps-helia"
              echo "\`\`\`"
              
            } > "$OUTPUT_FILE"
            
            echo "Custom output written to $OUTPUT_FILE"

    apply:
      steps:
        - env:
            name: APP_FILTER
            command: echo "$COMMENT_ARGS" | tr -d '\\'
        - run: |
            set -euo pipefail
            ENV="helia"
            echo "Applying Atlantis plan for ENV=$ENV with APP_FILTER=$APP_FILTER"
            chmod +x ./scripts/apply-plans.sh
            ./scripts/apply-plans.sh "$ENV" "$APP_FILTER"